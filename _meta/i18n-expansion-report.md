# TOPDA 외국인 중심 다국어화 — 작업 리포트

**작업 브랜치**: `claude/topda-multilingual-expansion-utbtls`
**작성일**: 2026-07-19
**범위**: 언어 4개 확장(총 6개), 외국인 관점 재구성, 계산기 대출 현실 반영, 용어집 확장, 자동화

> 한국어 원본과 기존 SEO/i18n 골격은 변경하지 않았고(계산 로직 불변), 추가·재구성 방식으로 작업했습니다.
> 콘텐츠 번역은 **기계 초벌**이며 각 페이지 상단에 `i18n:review` 주석을 달아 원어민 검수 대상으로 표기했습니다.

---

## 1. 지원 언어 (6)

| 코드 | 국기(SVG) | 표기 | 상태 |
|---|---|---|---|
| ko | 🇰🇷 `flags/ko.svg` | 한국어 | 원본(유지) |
| en | 🇺🇸 `flags/en.svg` | English | 기존 + 스위처 개편 |
| zh-Hans | 🇨🇳 `flags/zh-Hans.svg` | 简体中文 | **신규 홈** |
| zh-Hant | 🇹🇼 `flags/zh-Hant.svg` | 繁體中文 | **신규 홈** |
| vi | 🇻🇳 `flags/vi.svg` | Tiếng Việt | **신규 홈** |
| th | 🇹🇭 `flags/th.svg` | ภาษาไทย | **신규 홈** |

### 언어 스위처 (1단계)
- 헤더 `KR / EN` 텍스트 토글 → **지구본 아이콘 + 드롭다운**(모바일은 하단 시트)으로 교체.
- `app.js` 에서 전 페이지 자동 주입(개별 HTML 수정 불필요). 기존 정적 `.lang-switch` 는 자동 제거·대체.
- 국기는 **이모지가 아닌 SVG**(일부 OS 미렌더링 회피). 현재 선택 언어는 ✓ + 굵게 강조.
- 접근성: `aria-haspopup/expanded`, `role=menu/menuitem`, 키보드(↑↓/Home/End/Esc), 외부 클릭·백드롭 닫기.
- URL은 `/{lang}/` 하위 경로 기반(브라우저 저장 미사용, 기존 정책 준수). 번역이 없는 페이지는 **해당 언어 홈으로 폴백**해 404 방지.

---

## 2. 언어별 콘텐츠 우선순위 재구성 (2단계)

매핑 파일: `site/i18n/content-priority/{lang}.yaml` (rank=상단일수록 낮음, hidden=노출 제외).

| 언어 | 프로파일 | 상단 배치(요약) | hidden(외국인 미해당) |
|---|---|---|---|
| **en** | 서구권·실거주+매입 | 매입 절차 → 취득세 → 양도세 → 외국인 규제 → 이중과세 → 등기부 | 대출한도·청약 |
| **zh-Hans** | 중국 본토·중저가 매입 | 매입 절차 → 취득세 → 취득 신고 → 자금 반출입 → 매매 전 서류 | 대출한도·청약 |
| **zh-Hant** | 대만·홍콩·화교 | 매입 절차 → 취득세 → **台港 세제 차이** → 취득 신고 → 자금 | 대출한도·청약 |
| **vi** | 임대차 실수요 | 임대차보호법 → 표준계약서 → 전세사기 → 보증금 반환 → (매입은 하위) | 대출한도·양도세·청약 |
| **th** | 임대차+단기 | 임대차보호법 → **단기계약 유의** → 보증금 분쟁 → 표준계약서 | 대출한도·양도세·청약 |

- 각 언어 홈페이지의 "Quick start / 从这里开始 / Bắt đầu từ đây" 카드가 위 순서로 정렬됨.
- **계산기 실제 번역(업데이트)**: 핵심 계산기 3종(전월세 환산·중개보수·취득세)을 **4개 언어(zh-Hans/zh-Hant/vi/th)로 실제 번역**해
  `site/{lang}/calculators/`에 신설(계산기 index 포함, 언어당 4페이지 × 4언어 = 16페이지). 홈에서 영문 링크가 아니라 자국어 계산기로 연결.
  - 계산 로직은 `app.js` 그대로 재사용(언어 무관 동일). 생성기 `scripts/gen_calc_pages.mjs`로 일관 생성 → 신규 언어 추가 용이.
  - `app.js`의 통화/동적 문구를 한국어 외 전 언어에서 국제 표기(**KRW**·영문)로 렌더하도록 일반화(`isEn = lang !== 'ko'`).
  - jsdom 스모크 테스트로 3개 언어 계산기 실제 동작·KRW 출력·계산값 검증 완료.

- **후속 배치(잔금정산·양도세 추가 + 동적 문구 현지화)**: content-priority.yaml 순위에 따라 추가 진행.
  - `balance-settlement`(잔금일 정산) → en/zh-Hans/zh-Hant/vi/th 5개 언어 전부 신설.
  - `transfer-tax`(양도소득세) → en/zh-Hans/zh-Hant 3개 언어 신설(vi/th는 매핑상 hidden이라 제외).
  - **동적 계산 결과 문구의 한국어 하드코딩 수정**: 기존에는 `acquisition-tax`의 시나리오 레이블
    (예: "1주택 표준세율"), `transfer-tax`의 세율/면제 안내, `balance-settlement`의 정산 방향("매수자 → 매도자")이
    계산 로직 내부에 **한국어로 고정**되어 있어, 번역된 페이지에서도 이 부분만 한국어로 노출되는 문제가 있었음.
    계산 로직(세액·금액 산출)은 전혀 건드리지 않고, 결과에 `scenarioKey`/구조화 필드를 병행 반환하도록 확장한 뒤
    UI 레이어에서 언어별 사전(`SCENARIO_I18N`, `TT_I18N`, `ARROW`)으로 표시 문구만 조립하도록 분리.
    한국어 페이지 출력은 jsdom으로 수정 전후 동일함을 확인(회귀 없음).
  - **hreflang 정확성 버그 수정**: 생성기가 실제로 존재하지 않는 언어(예: vi/th의 transfer-tax)에도
    hreflang을 걸어 죽은 링크를 만들던 문제를 `availableLangsFor()`로 수정 — 실재하는 페이지만 상호 연결.
  - EN 계산기 index(`en/calculators/index.html`, 수기 관리 페이지)는 재생성 스크립트가 건드리지 않고
    새 카드 2개만 수동 삽입, content-priority 순서(취득세→양도세→중개보수→전월세→잔금정산→경매)로 재배열.
  - 4개 언어 홈의 계산기 스트립도 우선순위 순서로 신규 계산기 카드 추가.
  - jsdom으로 acquisition-tax(zh-Hans)·transfer-tax(en, 일반/비과세)·balance-settlement(vi) 재검증 통과.

---

## 3. 외국인 대출 가능/불가 현황 (3단계)

> ⚠️ **모든 항목 `verified:false`** — 현재 공식 소스로 독립 확인하지 못했습니다(프록시 403).
> 페이지에는 사실로 단정하지 않고 "unconfirmed / 확인 필요" 배지 + **Verify 링크**로만 노출했습니다.
> 데이터 원본: `site/assets/foreigner-loan-eligibility.json`.

| 상품 | 외국인 이용(초안) | 확인 소스 | 검증 |
|---|---|---|---|
| 주택담보대출(구입) | 조건부 — F-2/4/5/6·일부 취업비자, 소득증빙 | [KB](https://obank.kbstar.com/) | 미검증 |
| 전세자금대출 | 조건부 — **SGI 서울보증 위주(재호님 제공)** | [SGI](https://www.sgic.co.kr/) · [HUG](https://www.khug.or.kr/) · [HF](https://www.hf.go.kr/) | 미검증·확인필요 |
| 전세보증금 반환보증 | 외국인 취급 여부 불명 | [HUG](https://www.khug.or.kr/) · [SGI](https://www.sgic.co.kr/) | 미검증 |
| 정책대출(디딤돌·버팀목·보금자리) | 일반적으로 **배제**(내국인 세대 요건) | [HF](https://www.hf.go.kr/) | 미검증 |
| 주택청약 | 일반적으로 **배제** | [MOLIT](https://www.molit.go.kr/) | 미검증 |

- **UI 반영**: `en/foreigner-loan.html` 상단에 "Eligibility at a glance" 표 + 미확인 경고. 각 언어 홈에 대출 제약 콜아웃.
- **계산기 배지**: 대출 계산기(loan-limit·dsr·loan-compare·rti)에 외국인 제약 안내 배너를 `app.js`가 주입(계산 로직 불변).
- **핵심 확인 필요(재호님)**: "외국인 전세대출 = SGI 서울보증 전용"이 현재도 유효한지 공식 확인.

---

## 4. 용어집 (4단계)

- `site/i18n/glossary.json` — 5개 카테고리(비자·체류/임대차/매매·소유/대출·보증/기관·서류), **40개 용어 × 6개 언어**.
- 각 용어에 처리 방식(`method`) 근거: 직역 / 음역+설명 / 현지 유사개념 차용 / 코드·약어 유지.

### 확정 보류(pending) — 원어민·법률 검수 필요 7건
| id | 한국어 | 사유 |
|---|---|---|
| geoso | 거소증(국내거소신고증) | vi/th 공식 명칭 불확실 |
| hwakjeongilja | 확정일자 | 각국 유사 개념 매핑 확정 필요 |
| daehangnyeok | 대항력 | 법률 개념 — 직역 vs 현지 개념 차용 판단 |
| useonbyeonje | 우선변제권 | 동일 |
| imdaechaboho | 주택임대차보호법 | 법령명 공식 번역 확정 필요 |
| gongsijiga | 공시지가/공시가격 | 용어 구분(지가/가격) 처리 |
| jongbuse | 종합부동산세 | 제도 설명형 번역 확정 필요 |

---

## 5. 자동화 (5단계)

| 워크플로 | 스케줄(KST) | 역할 |
|---|---|---|
| `i18n-maintenance.yml` | 매월 1일 **09:30** | 사이트맵 i18n 보강, 정합성·커버리지 점검, 외부 링크 점검, 오류 시 이슈 |
| `policy-watch.yml` | 매월 1일 **10:00** | SGI/HUG/HF/MOLIT/KB 스냅샷 비교 → 변경 시 이슈(자동 반영 금지) |

- 기존 일일 갱신(00:00/00:10/00:30 KST)·대시보드(평일 07:00 KST)와 **시간대 미충돌**.
- 스크립트는 순수 Node(무의존): `gen_i18n_sitemap` · `i18n_check` · `link_check` · `policy_watch`.
- **드라이런 결과**: 로컬 실행 시 스크립트 정상 동작(정책/링크 점검은 이 환경 프록시 403으로 unreachable 처리 — CI 네트워크에서 baseline 채워짐). i18n_check 통과(오류 0).

---

## 5-2. 가이드·용어집 실번역 (후속 배치)

계산기에 이어, 4개 신규 언어 홈에서 "(EN)"으로 남아있던 핵심 가이드/용어집 링크를 실제 번역판으로 교체했다.

| 페이지 | 신규 언어 | 근거(content-priority) |
|---|---|---|
| `glossary.html` | zh-Hans·zh-Hant·vi·th (4개) | 전 언어 공통 필요 콘텐츠. `glossary.json` 데이터에서 자동 생성(검색 필터 JS 포함) |
| `jeonse.html` (Jeonse 101) | vi·th (2개) | vi/th 1순위 콘텐츠(임대차보호) |
| `foreigner-loan.html` | zh-Hans·zh-Hant·vi·th (4개) | 전 언어 공통 필요 콘텐츠. 3단계 대출 현실표(전부 `verified:false`) 그대로 유지 |
| `foreigner-tax.html` | zh-Hans·zh-Hant (2개) | zh 1순위 콘텐츠(세제 차이) |

- 용어집은 `scripts/gen_glossary_pages.mjs`로 데이터 기반 생성(카테고리 라벨 4개 언어 추가), jsdom으로 4개 언어 전부 검색·카운트 동작 검증.
- `foreigner-loan.html`은 3단계에서 확인한 "미검증(unconfirmed)" 배지·소스 링크를 그대로 유지 — 번역 과정에서 대출 가능/불가 판단을 새로 창작하지 않음(사실 추측 금지 원칙 준수).
- 4개 언어 홈의 관련 카드·풋터 링크를 네이티브 페이지로 교체하고 `(EN)` 라벨 제거. `en/*.html` 4종에 신규 언어 hreflang 추가(기존엔 없었음).
- `app.js` PAGES 매니페스트·sitemap 갱신, 내부 링크 전수 점검 통과, jsdom으로 skip-link·언어 스위처 정상 주입 확인.
- 아직 미번역: `about.html`, `auction.html` 등 나머지 EN 전용 페이지, 그리고 zh 대상 `foreigner-tax.html`이 vi/th에는 우선순위상 배정되지 않음(임대차 중심 프로파일이라 의도적 제외).

## 5-3. 가이드 허브·시세정보 실번역 (후속 배치)

지금까지 신규 언어 페이지의 상단 네비게이션에서 **가이드(Guides)**와 **시세정보(Market)** 링크가 한국어 원본으로 폴백돼 있던 문제를 해결했다. 두 허브 페이지를 각 언어로 번역해 신설하고, 네비게이션을 로컬 페이지로 연결했다.

| 페이지 | 신규 언어 | 성격 |
|---|---|---|
| `guides.html` | zh-Hans·zh-Hant·vi·th (4개) | 언어별 번역 완료 페이지를 content-priority 순으로 모은 허브. en/guides.html(수기 관리)은 유지 |
| `market.html`(시세정보) | en·zh-Hans·zh-Hant·vi·th (5개) | 맞춤찾기·실거래·지역시세·전세가율·상가시세 도구 허브. 라벨·설명만 번역 |

- **생성기 방식**: `scripts/gen_i18n_hubs.mjs` 로 데이터 테이블 기반 생성(계산기·용어집 생성기와 동일한 철학). 헤더/푸터는 각 언어 index.html 과 동일한 depth-1 상대경로라 그대로 재사용.
- **가이드 허브**: 각 언어에 실재하는 번역 페이지(jeonse·foreigner-loan·(zh)foreigner-tax·glossary·calculators·market)만 카드로 연결. 심화 한국어 가이드는 브라우저 번역 안내와 함께 한국어 허브로 링크.
- **시세정보 허브**: 실거래·지도 등 데이터 도구는 **한국어 데이터 앱을 그대로 링크**(지도·숫자는 언어 무관)하고, "데이터 도구는 한국어 표기 → 브라우저 번역 권장" 안내를 명시. **시세·가능/불가 등 사실·수치를 새로 창작하지 않음**(사실 추측 금지 원칙). as-of 표시 JS 문구만 언어별로 현지화, 데이터 경로는 `../assets/*.json` 으로 보정.
- **네비게이션 배선**: `scripts/rewrite_hub_nav.mjs` 로 언어판 56개 파일의 `../(../)guides|market.html` → 로컬 링크로 교체. en/guides.html 본문의 "한국어 가이드 허브" 링크는 의도된 것이라 예외 처리.
- **매니페스트·SEO**: `app.js` PAGES 에 5개 언어 `guides.html`/`market.html` 등록(스위처 딥링크), `gen_i18n_sitemap.mjs` REQUIRED 확장 후 sitemap 9건 추가. 두 허브 모두 6개 언어 hreflang(+x-default→ko) 상호 연결.
- **검증**: `i18n_check` 오류 0·경고 0, 신규 9개 페이지 태그 밸런스·인라인 JS `node --check` 통과, guides/market 링크 전수 해석 확인.

## 5-4. 맞춤 내집 찾기(search) 실번역 (후속 배치)

시세정보 허브의 대표 CTA인 **맞춤 내집 찾기**가 한국어 원본으로만 열리던 문제를 해결했다. 인터랙티브 앱 자체를 5개 언어로 번역해 `site/{lang}/calculators/search.html` 로 신설했다.

- **로직 불변·데이터 유지**: 점수 엔진 `score.js`·지역 유틸 `region.js` 는 공유·불변. 아파트·지역·역·학교명은 실데이터라 한국어 유지(지도·좌표 동일).
- **score.js 가법 확장**: 추천 이유를 한국어 문자열(`t`)과 함께 **구조화 데이터(`reasonData` key+파라미터)** 로도 반환하고, 근접추천 `relaxedKeys` 를 추가. 한국어 페이지 출력은 그대로(회귀 없음, node 검증). 번역 페이지는 이 구조화 데이터로 현지어 칩을 조립.
- **표시 문자열 전량 번역**: 폼 라벨·정렬·평형·업무지구(값=한국어, 표시=로마자/현지)·점수 산정표(8행)·결과 카드(순위·스펙·바·이유 칩)·상세 팝업·빈/완화 상태 문구까지. 통화는 국제 표기(zh: `亿`, en·vi·th: 십억 `₩`)로 렌더 — 예: 23억 → `₩2.3B` / `23亿` / `₩2.3 tỷ` / `₩2.3 พันล้าน`.
- **생성기**: `scripts/gen_search_pages.mjs`(언어별 사전 `T` + 정적 HTML) + `scripts/search_app.js.txt`(공유 인라인 JS, `T` 기반). 계산·데이터 fetch 경로는 depth-2(`../../assets/*`)로 보정.
- **배선**: 시세정보 허브 hero·각 언어 홈 파인더 카드 → 로컬 search 로 연결. `app.js` PAGES 5개 언어 등록, sitemap 5건 추가, 6개 언어 hreflang(+x-default→ko).
- **검증**: `i18n_check` 0/0, 조립된 인라인 JS `node --check` 통과, `score.js` 한국어 출력 회귀 없음 확인, fmt·이유칩 로직을 실제 `score.js` 결과로 4개 언어 단위 검증, 정적 링크 전수 해석.

## 5-5. search 사용성 개선 + 인테리어 가이드 (후속 배치)

**(A) 맞춤 내집 찾기 피드백 반영** — 실제 브라우저(Playwright/Chromium) 검증까지 완료.
- **클릭이 안 되던 버그 수정**: 상세 팝업 `.apt-modal*` 스타일이 한국어 원본에 인라인으로만 있어 번역 페이지에서 누락 → 모달이 스타일 없이(사실상 안 보이게) 떠서 "클릭이 안 된다"로 보였음. 생성기 STYLE 에 모달 CSS 편입.
- **Region 드롭다운 영문화**: 한글→개정 로마자 변환기(초/중/종성 테이블)로 시·도/시·군·구/동을 로마자 표기(예: 서울 강남구→`Seoul Gangnam-gu`, 대구→`Daegu`, 서귀포시→`Seogwipo-si`). option value 는 한글 유지 → 필터 로직 불변. 카드·팝업의 지역 라벨도 동일 적용.
- **금액 단위 국제화**: en·vi·th 는 **십억 KRW 단위 입력**(접미사 `B ₩`/`tỷ ₩`/`พันล้าน`), zh 는 `亿` 유지. 언어별 입력 배율(`budgetScale`)로 필터 정확도 유지(브라우저 검증: 예산 ≤6B → 결과 전부 ≤₩6B).
- **한글 검색 안내 메모**: 검색은 한글 매칭만 되므로 "Helio" 로는 안 나옴 → 한글명(예: 헬리오시티) 입력 안내를 필드 하단 메모로 추가, placeholder 도 한글 예시로 교체.
- **상세 팝업 지도 카카오맵 전환**: Leaflet 직접 호출 → 공용 어댑터 `kmap.js`(카카오 우선, 도메인 미등록·로컬은 Leaflet/OSM 폴백) 사용. 어댑터에 `setCenter`/`relayout` 가법 추가. 데이터 라벨의 `㎡(N평)` 은 현지 단위(`py`/`坪`/`pyeong`)로 표기.

**(B) 인테리어 가이드 번역** — `site/{lang}/interior/index.html` 5개 언어 신설(`scripts/gen_interior_pages.mjs`).
- 한국어 허브 구조(히어로·자재 6종 카드+비용·업체 선택 8단계 타임라인·공식자료/분쟁조정·표준 스케줄·면책)를 그대로 옮기고 문구만 번역.
- 자재 상세(cost/flooring/…)·관련 글·체크리스트는 **국내(한국어) 콘텐츠라 그대로 링크**하고 상단에 "상세는 한국어 → 브라우저 번역 권장" 안내. **시세·시공비 등 사실·수치 창작 금지**(원본 참고치·면책 유지).
- 각 언어 홈의 인테리어 카테고리 타일 → 로컬 인테리어 가이드로 연결. `app.js` PAGES 5개 등록, sitemap 5건 추가, 6개 언어 hreflang(+x-default→ko).
- **검증**: `i18n_check` 0/0, 인테리어 링크 전수 해석, en 페이지 브라우저 렌더 무오류.

## 6. 검증 요약

- **기능**: 6개 언어 스위처 링크 계산 로직 단위 검증(존재 시 딥링크, 부재 시 언어 홈 폴백). 국기 SVG XML 유효.
- **콘텐츠**: 언어별 상단 노출이 프로파일대로 재정렬. 외국인 미이용 대출은 배지/콜아웃으로 표기.
- **SEO**: 6개 언어 홈 hreflang 상호 연결(ko/en/zh-Hans/zh-Hant/vi/th + x-default), sitemap 갱신.
- **자동화**: 워크플로 YAML 파싱 OK, 스크립트 드라이런 OK.

---

## 7. 수동 후속 조치 체크리스트

- [ ] **원어민 검수**: zh-Hans/zh-Hant/vi/th 홈 + 용어집 pending 7건 + 신규 가이드/시세정보 허브 9종 (기계 초벌 → 검수).
- [ ] **외국인 대출 사실 확정**: SGI 전용 여부 등 공식 소스 확인 후 `foreigner-loan-eligibility.json` `verified:true` 전환.
- [ ] **국기 SVG 라이선스**: 현재 자체 제작 단순 SVG. 프로덕션은 `flag-icons`(MIT)로 교체 검토 — 국기 도안 자체는 저작권 비대상이나 아이콘 셋 라이선스 명시 권장.
- [ ] **도메인 hreflang/서치콘솔**: 배포 후 Google/Naver 서치콘솔에 sitemap 재제출, hreflang 인덱싱 확인.
- [ ] **언어별 심화 페이지**: 현재 신규 4개 언어는 홈만 존재(딥링크는 영문/한국어로 폴백). 수요 높은 페이지(임대차보호법·취득세 등)부터 언어판 추가 시 `app.js`의 PAGES 맵과 content-priority에 반영.
- [ ] **계산기 현지화**: 필요 시 언어별 계산기 UI 문자열 번역(로직은 공유).
- [ ] **GitHub Secrets/권한**: 워크플로 이슈 생성은 기본 `GITHUB_TOKEN` 사용. 조직 설정에서 Actions의 이슈 쓰기 권한 확인.
